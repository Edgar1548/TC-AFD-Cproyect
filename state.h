//
// Created by EDGAR on 10/06/2021.
//

#ifndef AFD_PROYECT_STATE_H
#define AFD_PROYECT_STATE_H
#include "library.h"

struct state{
    int position;
    vector<state*> states;
    vector<state*> states_zero;
    vector<state*> states_one;
    state* zero;
    state* one;
    string name;
};

void functionga(){
    unordered_map<state, tuple<state, int>>
};
queue.push(tuple<state,int>);
while (!queue.empty){
    if (state_a.name.size = 1 || state_b.name.size=1)
        break;
    state state_a = hallar_a;
    state state_b = hallar_b;
    map2[state]==true;
    if(map2[state_a]==false)  queue.push({state_a,1});
    if(map2[state_b]==false) queue.push({state_b,0});
    queue.pop_front();
    map[queue.front()] = {state,queue.front().first};
    state = queue.front().first;
}
string cadena;
while (map.find(state)==map.end()){
    cadena += to_string(map[state].second)
    state = map[state].first;
}





#endif //AFD_PROYECT_STATE_H
